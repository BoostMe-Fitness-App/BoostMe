import 'package:flutter/material.dart';

class YogaWelcomeScreen extends StatelessWidget {
  const YogaWelcomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Welcome to yoga'),
    );
  }
}
